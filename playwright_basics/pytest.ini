[pytest]
# Configure asyncio mode for pytest-asyncio
asyncio_mode = auto

# Specify test paths
testpaths = tests

# Specify test files (glob)
python_files = test_*.py

# Register custom markers
markers =
    asyncio: mark a test as an async test